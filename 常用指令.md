# 1.开发板初步学习

  ## 1. 网络信息

  - 开发板 IP：`172.50.0.234`
  - 虚拟机（Ubuntu）IP：`172.50.0.233`

---

## 2.Linux常用指令

1. 文件与目录

   | **指令**    | **常用参数**                                  | **说明**                         |
   | ----------- | --------------------------------------------- | -------------------------------- |
   | **`ls`**    | `-l` (详细), `-a` (显示隐藏), `-h` (易读大小) | 列出目录内容                     |
   | **`cd`**    | `..` (上级), `~` (主目录), `-` (上次目录)     | 切换工作目录                     |
   | **`pwd`**   | 无                                            | 显示当前完整路径                 |
   | **`mkdir`** | `-p` (递归创建多级目录)                       | 创建新目录                       |
   | **`rm`**    | `-r` (递归删除目录), `-f` (强制删除)          | 删除文件或目录（**慎用 `-rf`**） |
   | **`cp`**    | `-r` (拷贝目录), `-p` (保留属性)              | 复制文件或目录                   |
   | **`mv`**    | 无                                            | 移动或重命名文件/目录            |
   | **`touch`** | 无                                            | 创建空文件或更新文件时间戳       |

2. 文件内容查看

   **`cat`**: 一次性显示文件全部内容。

   **`less`**: 分页查看长文件（支持上下滚动，按 `q` 退出）。

   **`head` / `tail`**:

   - `tail -n 20 file`: 查看最后 20 行。
   - **`tail -f file`**: **实时刷新**查看文件末尾（常用于看日志）。

   **`grep`**: 强大的文本搜索工具。

   - `grep "error" log.txt`: 在文件中搜索关键词。
   - `ps -ef | grep python`: 结合管道符过滤进程。

   **`find`**: 在文件系统中查找文件。

   - `find /path -name "*.conf"`: 按文件名搜索。

3. 系统状态与进程

   **`top` / `htop`**: 实时显示系统资源占用（CPU、内存）及进程列表。

   **`ps`**: 静态查看进程快照。

   - `ps -ef` 或 `ps aux`: 显示所有运行中的进程。

   **`kill`**: 终止进程。

   - `kill -9 <PID>`: 强制杀死指定进程。

   **`df`**: 查看磁盘剩余空间。

   - `df -h`: 以 GB/MB 格式显示。

   **`free`**: 查看内存使用情况。

   - `free -h`: 易读格式显示。

4. 网络相关

   **`ping`**: 测试网络连通性。

   **`ifconfig`** (或 `ip addr`): 查看本机的 IP 地址和网卡信息。

   **`netstat`**: 查看网络端口占用。

   - `netstat -tuln`: 查看当前系统监听的端口。

   **`curl` / `wget`**:

   - `curl -X GET http://example.com`: 发送网络请求。
   - `wget http://url/file.zip`: 下载文件。

5. 权限相关

   **`chmod`**: 修改文件权限。

   - `chmod 755 file`: 设置为 rwxr-xr-x。
   - `chmod +x script.sh`: 赋予执行权限。

   **`chown`**: 修改文件所有者。

   - `chown user:group file`: 更改属主和属组。

   **`sudo`**: 以超级用户（root）身份执行命令。

6. 压缩与归档

   **`tar`**:

   - 解压：`tar -zxvf file.tar.gz`
   - 压缩：`tar -zcvf output.tar.gz /path/to/dir`

   **`zip` / `unzip`**: 处理 .zip 格式文件。
   
   
   
   ### Linux手册
   
   Linux 手册的 9 大核心章节：
   
   | **章节号** | **代表内容**     | **详细说明**                                           | **学习场景示例**                           |
   | ---------- | ---------------- | ------------------------------------------------------ | ------------------------------------------ |
   | **1**      | **用户命令**     | 终端里普通的 shell 命令。                              | `man 1 ls` (查看 ls 命令怎么用)            |
   | **2**      | **系统调用**     | **内核**提供的函数。这是 C 语言底层开发最常查的！      | `man 2 open` (查系统底层打开文件的函数)    |
   | **3**      | **C 库函数**     | C 语言标准库（glibc 等）提供的函数。                   | `man 3 fopen`、`man 3 printf`              |
   | **4**      | **特殊文件**     | 设备文件或驱动程序的说明（通常在 `/dev` 下）。         | `man 4 fb0` (查屏幕设备文件)               |
   | **5**      | **配置文件格式** | 系统配置文件的格式说明。                               | `man 5 passwd` (查 /etc/passwd 文件的格式) |
   | **6**      | **游戏**         | 终端小游戏的说明（现在较少见）。                       | -                                          |
   | **7**      | **杂项**         | 宏包、网络协议、文件系统等各类约定规范。               | `man 7 ip` (查看 IPv4 协议说明)            |
   | **8**      | **系统管理命令** | 通常是 root 用户（管理员）使用的系统维护命令。         | `man 8 fdisk` (磁盘分区工具)               |
   | **9**      | **内核例程**     | Linux 内核源代码级别的函数文档（非标准，平时极少用）。 |                                            |
   
   

  ## 3. 写代码流程

    1. 写代码：`xxx.c`
    2. 在 Ubuntu 编译：
    
    Ubuntu输入不显示问题
    输入 stty echo

  ```bash
aarch64-linux-gnu-gcc xxx.c
# 默认生成 a.out


3. 传输可执行文件到开发板（按你的笔记方式）：

rz -y
chmod +x 可执行文件名

4. 运行代码：

./可执行文件名

———

## 3. TFTP 指令

sudo service tftpd-hpa start    # 启动 tftp
sudo service tftpd-hpa stop     # 停止 tftp
sudo service tftpd-hpa restart  # 重启 tftp

> 注意：一般第一次传输文件，或很久没有用 tftp 传输时，建议先重启 tftp。

———

## 4. 虚拟机网络图标异常处理

sudo service network-manager stop
sudo rm /var/lib/NetworkManager/NetworkManager.state
sudo service network-manager start
sudo gedit /etc/NetworkManager/NetworkManager.conf
# 将文件里唯一的 false 改成 true
sudo service network-manager restart

———

## 5. 上传 a.out 到开发板（网络已通）

1. 把文件放到 tftp 目录：

cp a.out /home/china/tftpboot


2. 在 SecureCRT（开发板端）执行下载到开发板：

tftp -g -r main.c 172.50.1.92
# 说明：从 Ubuntu IP 下载文件到开发板

3. 开发板上传文件到 Ubuntu：

tftp -p -l 1.c 172.50.1.99
———
  ```

------

## 4. 库文件 / Makefile / CMake 相关指令

1. 动态库 (Shared Library, .so) 
    核心原理：运行期加载，多程序共享，极大地节省开发板内存和磁盘空间。

  ```makefile
  # 1. 将多个源文件编译为动态库 (必须加 -fPIC 和 -shared)
  # 规范：输出文件必须以 lib 开头，以 .so 结尾
  gcc -shared -fPIC bst.c linkqueue.c -o libbst.so
  
  # 2. 编译主程序并链接动态库
  # -I ./ : 告诉编译器去当前目录找头文件 (如 bst.h)
  # -L ./ : 告诉链接器去当前目录找库文件
  # -l bst : 链接库，掐头去尾 (去lib，去.so)
  gcc main.c -I ./ -L ./ -l bst -o app
  
  # 3. 运行前设置环境变量 (给操作系统动态加载器指路)
  export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:.
  ./app
  ```

  

2. 静态库 (Static Library, .a)
    核心原理：编译期直接将机器码拷贝进可执行文件中，体积大，但不依赖外界环境。

  ```makefile
  # 1. 先生成目标文件 (.o)
  gcc -c bst.c linkqueue.c
  
  # 2. 使用 ar 工具打包成静态库 (必须以 lib 开头，.a 结尾)
  ar rcs libbst.a bst.o linkqueue.o
  
  # 3. 链接主程序 (命令与动态库完全一样，GCC优先链接同名动态库)
  gcc main.c -I ./ -L ./ -l bst -o app_static
  ```

  

3. makefile

  核心痛点解决：替代手动敲击 gcc 的繁琐过程，实现增量编译（只编译修改过的文件）。

  ```makefile
  # ==========================================
  # 1. 变量定义区 (方便一键切换为交叉编译)
  # ==========================================
  # 如果要在开发板跑，把 gcc 改成 aarch64-linux-gnu-gcc
  CC = gcc  
  CFLAGS = -I./ -fPIC -Wall
  LDFLAGS = -L./ -lbst
  
  # ==========================================
  # 2. 终极目标区
  # ==========================================
  all: libbst.so app
  
  # ==========================================
  # 3. 规则定义区
  # 魔法变量速查：
  # $@ : 代表目标文件 (冒号左边)
  # $^ : 代表所有依赖文件 (冒号右边)
  # $< : 代表第一个依赖文件
  # ==========================================
  
  # 目标：生成动态库
  libbst.so: bst.c linkqueue.c
  	$(CC) -shared $(CFLAGS) -o $@ $^
  
  # 目标：生成可执行程序
  app: main.c libbst.so
  	$(CC) $< $(CFLAGS) $(LDFLAGS) -o $@
  
  # ==========================================
  # 4. 伪目标与清理
  # ==========================================
  .PHONY: clean
  clean:
  	rm -f app libbst.so *.o
  ```

  



1. 核心 CMakeLists.txt 模板
    无论工程怎么变，最核心的骨架只有这 6 句代码
    
    ```cmake
    # 1. 设定版本与项目名称
    cmake_minimum_required(VERSION 3.10)
    project(RK3568_Project)
    
    # 2. 指定头文件搜索路径 (等价于 -I)
    include_directories(${CMAKE_CURRENT_SOURCE_DIR})
    
    # 3. 生成动态库 (等价于 gcc -shared -fPIC...)
    # CMake 会自动补充 lib 前缀和 .so 后缀
    add_library(bst SHARED bst.c linkqueue.c)
    
    # 4. 生成可执行文件
    add_executable(app main.c)
    
    # 5. 链接依赖库 (等价于 -L 和 -l)
    # CMake 非常聪明，它知道 bst 在哪，全自动处理链接路径
    target_link_libraries(app bst)
    ```
    
    


2. CMake 标准执行流 (外部构建)
    严禁在源码目录下直接 cmake，务必保持源码目录干净：

    ```bash
    mkdir build
    cd build
    cmake ..
    make
    ```

    

3. Make执行指令注意

    ```bash
    make (-n)或(-s)
    	make首先在当前目录下面找Makefile/makefile,然后在该makefile中找第一个目标，执行
    
    make (-n)或(-s) 目标名
    	make首先在当前目录下面找Makefile/makefile,然后在该makefile中找目标，再去找依赖文件....再去执行达成目标命令列表
    	
    make -f makefile所在的路径名 目标名 	=>makefile的名字可以任意起
    	make首先会在makefile所在的路径去查找，然后再到makefile这个文件找 目标名 执行
    
    -n	只打印执行的命令，不显示结果
    -s	执行命令显示结果，但是不显示执行的命令
    ```

  

4. 内置函数

   ```makefile
   1. wildcard（通配符自动扫描）
   作用：像雷达一样，去指定的目录扫描所有符合规则的文件名，并用空格隔开。
   语法：$(wildcard 匹配模式)
   
   # 获取当前目录下所有的 .c 文件，存入变量 SRC
   SRC = $(wildcard *.c)
   
   2. patsubst（模式批量替换）
   作用：把一串文本中符合条件的单词，批量替换成你想要的格式（常用于把 .c 列表映射为 .o 列表）。
   语法：$(patsubst 查找模式, 替换模式, 目标文本串)
   # 把 SRC 变量里所有的 .c 替换成 .o，存入变量 OBJ
   OBJ = $(patsubst %.c, %.o, $(SRC))
   
   3. shell（执行外部系统命令）—— [高频补充]
   作用：在 Makefile 里直接运行 Linux 终端命令，并把结果拿回来。
   语法：$(shell 系统命令)
   # 获取当前所在的绝对路径
   CUR_DIR = $(shell pwd)
   # 获取系统的架构信息 (用于判断是不是 aarch64)
   ARCH = $(shell uname -m)
   
   4. notdir（剥离目录路径）—— [高频补充]
   作用：当你拿到的文件名带有很长的路径时，用它可以只保留最后的文件名。
   语法：$(notdir 包含路径的文件名列表)
   # 去掉 src/ 路径，只保留文件名
   FILES = $(notdir src/main.c src/add.c)
   # 此时 FILES 的值就是 "main.c add.c"
   ```

   

## 5.Makefile万能模板

1. Makefile万能模板

   ```makefile
   # 1. 编译器与目标名称定义
   # 【注意】如果你要放到 RK3568 开发板上跑，请将 gcc 改为 aarch64-linux-gnu-gcc
   CC = gcc
   #CC = aarch64-linux-gnu-gcc
   TARGET = app
   
   # 2. 编译选项与链接路径配置
   CFLAGS = -I./ -Wall -g      # -I 指定头文件路径，-Wall 开启警告，-g 支持GDB调试
   LDFLAGS = -L./              # -L 指定库文件路径 (如果需要链动态库，在后面加 -lxxx)
   LIBname = -l
   
   # 3. 魔法函数：自动处理源文件
   # 扫描当前目录下所有的 .c 文件
   SRC = $(wildcard *.c)
   # 将所有的 .c 文件名映射为对应的 .o 文件名
   OBJ = $(patsubst %.c, %.o, $(SRC))
   
   # 4. 终极编译规则 (链接成可执行文件)
   $(TARGET): $(OBJ)
   	$(CC) $^ -o $@ $(LDFLAGS)
   
   # 5. 模式编译规则 (教 make 如何把 .c 编译成 .o)
   %.o: %.c
   	$(CC) -c $< -o $@ $(CFLAGS)
   
   # 6. 防坑清理规则
   .PHONY: clean
   clean:
   	rm -f $(TARGET) $(OBJ)
   ```

   

2. Cmake万能模板

   ```cmake
   # 1. 指定 CMake 的最低版本要求
   cmake_minimum_required(VERSION 3.10)
   
   # 2. 设置编译路径（指定工具链）
   set(CMAKE_C_COMPILER /usr/local/arm/gcc-linaro-7.5.0-2019.12-x86_64_aarch64-linux-gnu/bin/aarch64-linux-gnu-gcc)
   set(CMAKE_CXX_COMPILER /usr/local/arm/gcc-linaro-7.5.0-2019.12-x86_64_aarch64-linux-gnu/bin/aarch64-linux-gnu-g++)
   
   # 2. 定义项目名称 (后续可以通过 ${PROJECT_NAME} 变量来引用它)
   project(RK3568_App)
   
   # 3. 设置全局编译选项 (相当于 Makefile 里的 CFLAGS)
   # 这里开启了所有警告，并加入了 GDB 调试信息支持
   add_compile_options(-Wall -g)
   
   # 4. 指定头文件的搜索目录 (相当于 Makefile 里的 -I ./)
   include_directories(${CMAKE_CURRENT_SOURCE_DIR})
   
   # 5. 魔法扫描：自动查找当前目录下所有的源码文件，并存入变量 DIR_SRCS 中
   aux_source_directory(. DIR_SRCS)
   
   # 6. 指定库文件所在的路径
   link_directories(./lib)
   
   # 7. 添加lib子目录
   add_subdirectory(./lib)
   
   # 8. 生成可执行文件
   # 使用上一步扫描到的所有源码，生成以项目名字命名的可执行文件
   add_executable(${PROJECT_NAME} ${DIR_SRCS})
   
   # 9. 链接依赖的动态库/静态库 (相当于 Makefile 里的 -L 和 -l)
   # 【注意】如果你的项目不需要链接额外的库，这一行可以直接注释掉
   target_link_libraries(${PROJECT_NAME} pthread m)
   ```
   
   

# 2.Linux文件操作

### PS：关开发板界面启动程序

1.连接开发板
2.ps -aux 查看后台运行的程序
	找到./rx356x-demo 看对应的pid是多少
3.结束程序
	kill -9 pid	#这个pid替换成上面./rx356x-demo的pid

省流版：killall -9 rk356x-demo

## 1.文件IO

1. **open**

   ```c
   #include <sys/types.h>
   #include <sys/stat.h>
   #include <fcntl.h>
   
   int open(const char *pathname, int flags);//打开一个已经存在的文件
   int open(const char *pathname, int flags, mode_t mode);//创建一个新的文件并打开
   参数：
   	pathname：要打开的文件或创建文件的路径(绝对路径或相对路径)
   	flags：打开文件标记
   		O_RDONLY只读  0
   		O_WRONLY只写	1
   		O_RDWR可读可写	2
   		注意：上面的三个状态标记只能选择其中的一个
   		O_CREAT文件如果不存在，则创建一个文件
   		O_TRUNC文件已经存在，则进行截短(变成空的文件)
   		O_APPEND追加，设置文件的偏移量会在文件末尾(写)
   		O_NONBLOCK以非阻塞的方式打开文件，默认是阻塞
   	eg:以只读的方式打开已经存在的文件1.txt
   		int fd = open("1.txt",O_RDONLY);
   	多个状态标记需要用到位或连接 |
       eg:打开一个已经存在的文件1.txt，并且以读写的方式打开，在文件的末尾写入数据
       	int fd = open("1.txt",O_RDWR|O_APPEND);
   	mode:指定创建文件的权限，当第二个参数有O_CREAT才会有第三个参数
   		创建新的文件的权限 = mode & ~mask
   		有两种指定权限的方式
   		方式1：八进制
   			0777 
   		方式2：宏
           	
   返回值：
   	成功：返回一个打开文件的文件描述符 >2
   	失败：-1，同时设置错误码
   ```

2. **perror**

   ```c
   void perror(const char *s);
   功能：打印s字符串并再在后面加上错误的原因，会自动换行
   eg：
   	perror("");//这句话在打开失败的时候打印错误原因，并且会自动换行
   ```

3. **umask**

   ```c
   #include <sys/types.h>
   #include <sys/stat.h>
   
   mode_t umask(mode_t mask);
   mask:指定文件的掩码
   返回值：返回上一次的掩码
   
   注意：
   	在共享目录下去设置umask,最后的新文件的权限可能会与预期不符
   	需要把运行的文件放到linux文件夹下运行 /home/china
   ```

4. **write**

   ```c
   #include <unistd.h>
   
   ssize_t  write(int fd, const void *buf, size_t count);
   功能：把buf所指向的空间中count个字节 写入到fd所代表的文件中
   参数：
   	fd:写入的那个文件的描述符
   	buf:指针，指向的空间用来保存写入到文件中的数据
   	count:你想要写入多少个字节到文件中
   返回值：
   	成功：返回值实际要写入的字节数(<=count)
   	失败：-1，errno被设置
   ```

5. **read**

   ```c
   #include <unistd.h>
   
   ssize_t read(int fd, void *buf, size_t count);
   功能：从fd所代表的文件中读取count个字节数据放到buf所指向的空间中
   参数：
   	fd:读取的那个文件的描述符
   	buf:指针，指向的空间用来保存从文件中读取到的数据
   	count:你想要读取多少个字节放到buf所指向的空间中
   返回值：
   	成功，返回实际读取到的字节数(<=count)
   	返回0：已经读到文件的末尾EOF(end if file)
   	失败：-1，error被设置
   ```

6. **lseek**

   ```c
   #include <sys/types.h>
   #include <unistd.h>
   
   off_t lseek(int fd, off_t  offset,  int  whence);
   功能：重新定位文件偏移量(光标)
   参数
   	fd:要进行重新定位光标的那个文件的fd
   	offset:偏移量，数字，具体的含义要结合第三个参数
   	whence：定位的方式，从哪个位置开始偏移
   		SEEK_SET:文件开头
   		SEEK_END:文件末尾
   		SEEK_CUR:当前光标的位置
   返回值
   	成功：返回新光标距离文件开头的字节数
   	失败：-1，error被设置
   eg:
   	//移动光标到文件开头
       lseek(fd,0,SEEK_SET);
   eg:
   	//获取文件的大小
   	int file_size = lseek(fd,0,SEEK_END);
   ```

   

## 2.进程目录

1. **获取进程当前工作目录**

   ```c
   #include <unistd.h>
   
   char *getcwd(char *buf, size_t size);
   buf:指向的空间保存获取到的字符串
   size:指向buf指向的空间，最多可以拷贝多少个字节
   	目录字符串的长度 > size-1  
       直接返回错误码，就不会出现数组越界的访问
   返回值：
   	成功：返回获取的目录字符串的地址
   	失败：NULL，设置错误码
   
   char *getwd(char *buf);
   buf:指向的空间保存获取到的字符串
   返回值：
   	成功：返回获取的目录字符串的地址
   	失败：NULL，设置错误码
   	
   char *get_current_dir_name(void);
   ```

2. **改变进程当前工作目录**

   ```c
   #include <unistd.h>
   
   int chdir(const char *path);
   path:要改变的工作路径
   返回值：
   	成功 0
   	失败 -1
   int fchdir(int fd);
   fd:需要使用open函数打开要改变的工作路径(打开目录应该需要只读权限)
   返回值：
   	成功 0
   	失败 -1
   ```



## 3.文件截短

```c
#include <unistd.h>
#include <sys/types.h>

int truncate(const char *path, off_t length);
path:文件名
length:截短的长度
	legth < 原来的长度
		截短
	legth > 原来的长度
		后面会给你补相应的字符(\0 nul)
		"扩展留下空洞"
返回值：
	成功 0
	失败 -1
int ftruncate(int fd, off_t length);
```

## 4.删除文件

```c
unlink 删除一个文件(除了目录外的其他文件)
rmdir  删除一个空目录

remove 用来删除一个普通文件或空目录
		->如果删除的是普通文件调用unlink
		->如果删除的是空目录调用rmdir

#include <unistd.h>

int unlink(const char *pathname);
int rmdir(const char *pathname);

#include <stdio.h>

int remove(const char *pathname);
```

## 5.获取文件属性

```c
#include <sys/types.h>
#include <sys/stat.h>
#include <unistd.h>

int stat(const char *pathname, struct stat *statbuf);
int fstat(int fd, struct stat *statbuf);
int lstat(const char *pathname, struct stat *statbuf);
pathname:带路径的文件名，用来指定要获取状态信息的哪个文件
statbuf:结构体指针，指向了一个struct stat类型的结构体空间，用来存放获取到的文件的属性信息的
fd:文件描述符，需要把文件用open打开
返回值：
	成功 0
	失败 -1 error被设置
eg:
	struct stat statbuf;
	stat("1.txt",&statbuf);//获取属性保存到statbuf
```

注意stat和lstat之间的区别

stat和fstat什么类型文件都可以使用这两个函数，lstat只能获取链接文件的属性

```c
来判断stat类型  man 2 stat
```

```c
判断文件类型st_mode   man 2 stat 或 man 7 inode 找相关示例
```

```c
    eg:
    	struct stat sb;//保存属性
        stat(pathname, &sb);
        if ((sb.st_mode & S_IFMT) == S_IFREG) {
        	printf("普通文件\n");
        }
        else if ((sb.st_mode & S_IFMT) == S_IFDIR) {
        	printf("目录\n");
        }
     eg:
    	struct stat sb;//保存属性
        stat(pathname, &sb);
        if (S_ISREG(sb.st_mode)) {
        	printf("普通文件\n");
        }
        else if (S_ISDIR(sb.st_mode)) {
        	printf("目录\n");
        }	
```

## 6.读取目录内容

1. **打开目录 opendir**

   ```c
   #include <sys/types.h>
   #include <dirent.h>
   
   DIR *opendir(const char *name);
   name:打开目录的路径
   返回值：
   	成功：返回一个DIR*指针，用来表示一个被打开的目录
   	失败：NULL，errno被设置
   DIR *fdopendir(int fd);
   ```

   

2. **读取目录readdir**

   ```c
   #include <dirent.h>
   
   struct dirent *readdir(DIR *dirp);
   dirp:一个打开的目录的指针
   返回值：
   	成功：指向下一个目录项的指针
   	失败：没有更多的目录或出错 NULL
   ```

3. **关闭目录closedir**

   ```c
   #include <sys/types.h>
   #include <dirent.h>
   
   int closedir(DIR *dirp);
   ```

   

# 3.屏幕显示

## 1.颜色显示

开发板的分辨率：1024*600

三基色：红  绿 蓝（每一个分量都是一个字节)

```
r		g		b
0xff	0x00	0x00	深红色0xff0000
0x00	0xff	0x00	深绿色0xff00
```

颜色显示的步骤

1.打开屏幕 "/dev/fb0"（开发板的路径）

```
int lcd_fd = open();
```

2.映射

```c
y=2*x+3;
把x当成是一块缓冲区(内存)，间接改变y的值,把y当成是设备
建立了映射关系，操作缓冲区就像操作设备一样的

mmap映射
#include <sys/mman.h>

void *mmap(void *addr, size_t length, int prot, int flags,
int fd, off_t offset);
addr:一般填NULL，想让系统给我们分配映射的地址，而不是自己指定
length:映射的内存空间需要多大
	1024*600*4
prot：映射过后的权限 PROT_EXEC|PROT_READ|PROT_WRITE
       PROT_EXEC  可执行
       PROT_READ  可读
       PROT_WRITE 可写
flags：映射过后的属性
	MAP_SHARED：共享属性，对内存的操作，立即再设备上显示
fd:内存要跟哪个文件建立映射关系
	lcd_fd
offset:偏移量
	0
返回值：
	成功：返回与设备建立映射关系的内存地址
	失败：NULL
```

3.显示颜色

```c
给第一个点赋值红色
*plcd = 0xff0000;
给第二个点赋值红色
*(plcd+1) = 0xff0000;
给任意一个(x,y)赋值红色 x:列 y:行
*(plcd+1024*y+x) = 0xff0000;

/*
	画点子函数
	i:x轴
	j:y轴
*/
void lcd_draw_point(int i,int j,unsigned int color)
{
	if(i>=0&&i<1024 && j>=0&&j<600)
		*(plcd+1024*j+i) = color;
}

for(int i = 0;i<600;i++)//行 y轴
{
	for(int j = 0;j<1024;j++)//列 x轴
	{
		//画点函数
		lcd_draw_point(j,i,0xff0000);
	}
}
```

4.解除映射

```c
int munmap(void *addr, size_t length);
参数：
	addr:释放刚才映射的地址
		plcd
	length:要释放刚才映射空间的大小
		1024*600*4
```

5.关闭屏幕

```c
close(lcd_fd);
```

## 2.图片显示



1. 打开图片

   ```
   int bmp_fd = open("1.bmp",O_RDWR);
   ```

2. 读取图片

   获取图片的宽 高 色深

   ```c
   int width = 0;
   lseek(bmp_fd,0x12,SEEK_SET);
   read(bmp_fd,&width,4);
   读到的width可能是一个负数
   	width > 0
   		从左到右保存每一个像素点
   	width < 0
   		从右到左保存每一个像素点
   int height = 0;
   lseek(bmp_fd,0x16,SEEK_SET);
   read(bmp_fd,&height,4);
   读到的height可能是一个负数
   	height > 0
   		从下到上保存每一个像素点
   	height < 0
   		从上到下保存每一个像素点
   int depth = 0;
   lseek(bmp_fd,0x1C,SEEK_SET);
   read(bmp_fd,&depth,2);
   depth == 24
   	rgb
   depth == 32
   	argb
   ```

3. c调色板

   ```
   对于24位图和32位图没有调色板
   ```

4. 像素数组

   ```c
   一行像素点的个数
   	abs(width)	//abs取绝对值
   每个像素点占多少个字节
   	depth/8
   一行有多少个字节
   	int line_bytes = abs(width)*depth/8
   为了保证每一行字节数都是4的倍数，就需要在末尾添加几个空白的字节
   	"癞子"
   每一行需要的癞子数量
   	int laizi = 0;
   	if(line_bytes%4)
   	{
   		laizi = 4-line_bytes%4;
   	}
   一行的总的的字节数
   	line_bytes += laizi;
   整个像素数组的大小
   	int total_bytes = line_bytes * abs(height);
   ```

   ```c
   char pix[total_bytes];//用来保存读到的像素数组的数据
   lseek(bmp_fd,54,SEEK_SET);//跳转光标到像素数组的位置 54
   read(bmp_fd,pix，total_bytes);//读取数据放到pix中
   ```

5. 解析像素数组

   ```c
   unsigned char a,r,g,b;
   unsigned int color;
   int x0 = 0,y0 = 0;//显示图片起始位置坐标
   int i = 0;
   for(int y = 0;y<abs(height);y++)//行  
   {
   	for(int x = 0;x<abs(width);x++)//列
   	{
   		b = pix[i++];
   		g = pix[i++];
   		r = pix[i++];
   		if(depth == 32)
   			a = pix[i++];
   		else if(depth == 24)
   			a = 0;
   		//组装一个颜色
   		color = a<<24|r<<16|g<<8|b;
   		//画点
   		lcd_draw_point(width>0?x+x0:abs(width)-x-1+x0,height>0?abs(height)-y-1+y0:y+y0,color);//x(列) y(行) 颜色
   	}
   	//跳过癞子
   	i+=laizi;
   }
   ```

6. 关闭图片

   ```
   close(bmp_fd)
   ```

   



# 4.文件标准IO

## **1. 标准 IO 是什么**

- 标准 IO 是 C 标准库提供的输入输出接口，头文件是 `stdio.h`
- 标准 IO 的核心对象是 `FILE *`
- 标准 IO 底层通常还是会调用系统 IO
- 标准 IO 的特点是带缓冲，使用更方便，很多场景下效率更高

**2. 标准 IO 和系统 IO 的区别**

| 对比项     | 标准 IO                               | 系统 IO                          |
| ---------- | ------------------------------------- | -------------------------------- |
| 头文件     | `stdio.h`                             | `unistd.h`、`fcntl.h`            |
| 操作对象   | `FILE *`                              | 文件描述符 `int fd`              |
| 典型函数   | `fopen`、`fclose`、`fgets`、`fprintf` | `open`、`close`、`read`、`write` |
| 是否带缓冲 | 带缓冲                                | 一般不由 C 库提供缓冲            |
| 使用难度   | 更方便                                | 更底层                           |

**3. 什么是流**

- 流就是数据传输的抽象
- 标准 IO 把文件、键盘、屏幕等统一看成“流”
- 常见标准流：
| 名称     | 含义       | 默认设备 |
| -------- | ---------- | -------- |
| `stdin`  | 标准输入流 | 键盘     |
| `stdout` | 标准输出流 | 屏幕     |
| `stderr` | 标准错误流 | 屏幕     |

**4. 缓冲区**

- 标准 IO 带缓冲区，数据通常先进入缓冲区，再真正写入文件或设备
- 使用缓冲区的目的是减少系统调用次数，提高效率

| 缓冲类型 | 特点                         |
| -------- | ---------------------------- |
| 行缓冲   | 遇到换行 `\n` 或缓冲满时刷新 |
| 全缓冲   | 缓冲区满时才刷新             |
| 无缓冲   | 数据尽快立即输出             |

## **2.常见标准IO函数**

**5. 文件流的打开和关闭**

函数原型：

```c
FILE *fopen(const char *pathname, const char *mode);
int fclose(FILE *stream);
```

`fopen` 参数说明：

- `pathname`：文件路径
- `mode`：打开方式

常见打开方式：

| 模式   | 含义                       |
| ------ | -------------------------- |
| `"r"`  | 只读，文件必须存在         |
| `"r+"` | 读写，文件必须存在         |
| `"w"`  | 只写，不存在创建，存在清空 |
| `"w+"` | 读写，不存在创建，存在清空 |
| `"a"`  | 追加写，不存在创建         |
| `"a+"` | 读写追加，不存在创建       |
| `"rb"` | 二进制只读                 |
| `"wb"` | 二进制只写                 |
| `"ab"` | 二进制追加                 |

注意：

- `fopen` 成功返回 `FILE *`
- `fopen` 失败返回 `NULL`
- `fclose` 成功返回 `0`
- `fclose` 失败返回 `EOF`

常见写法：

```c
FILE *fp = fopen("1.txt", "r");
if (fp == NULL)
{
    perror("fopen");
}
```

**6. 按字符读写文件**

函数：

```c
int fgetc(FILE *stream);
int fputc(int c, FILE *stream);
```

作用：

- `fgetc`：从文件中读一个字符
- `fputc`：向文件中写一个字符

返回值：

- 成功返回字符对应的 ASCII 值
- 失败或结束返回 `EOF`

注意：

- 接收 `fgetc` 返回值建议用 `int`
- 因为要区分普通字符和 `EOF`

示例：

```c
int ch;
while ((ch = fgetc(fp)) != EOF)
{
    fputc(ch, stdout);
}
```

**7. 按行读写文件**

函数：

```c
char *fgets(char *s, int n, FILE *stream);
int fputs(const char *s, FILE *stream);
```

作用：

- `fgets`：从文件中读一行
- `fputs`：向文件中写一个字符串

参数说明：

- `s`：保存字符串的数组
- `n`：最多读取 `n-1` 个字符
- `stream`：文件流

返回值：

- `fgets` 成功返回 `s`，失败返回 `NULL`
- `fputs` 成功返回非负值，失败返回 `EOF`

注意：

- `fgets` 读到换行时，通常会把 `\n` 一起读进来
- `fputs` 不会自动加换行
- `puts` 会自动加换行，但主要用于标准输出

去掉 `fgets` 读入的换行常用写法：

```c
name[strcspn(name, "\n")] = '\0';
```

**8. 格式化读写文件**

函数：

```c
int fscanf(FILE *stream, const char *format, ...);
int fprintf(FILE *stream, const char *format, ...);
```

作用：

- `fscanf`：按格式从文件读数据
- `fprintf`：按格式向文件写数据

例子：

```c
fprintf(fp, "id:%d name:%s score:%d\n", id, name, score);
```

```c
fscanf(fp, " id:%d name:%53s score:%d", &s.id, s.name, &s.score);
```

注意：

- `fscanf` 返回成功匹配的项数
- 例如有 3 个变量，就常写成 `== 3`
- 格式必须与文件内容匹配
- `%s` 最好限制长度，如 `%53s`

**9. 为什么 `fscanf` 前面常加空格**

例如：

```c
fscanf(fp, " id:%d name:%53s score:%d", &s.id, s.name, &s.score)
```

前面的空格表示：

- 跳过输入中的所有空白字符
- 包括空格、换行、`\t`

作用：

- 避免上一条记录后的换行影响下一次读取

**10. 二进制读写**

函数：

```c
size_t fread(void *ptr, size_t size, size_t nmemb, FILE *stream);
size_t fwrite(const void *ptr, size_t size, size_t nmemb, FILE *stream);
```

作用：

- `fread`：按块读取二进制数据
- `fwrite`：按块写入二进制数据

参数说明：

- `ptr`：内存地址
- `size`：每个元素大小
- `nmemb`：元素个数
- `stream`：文件流

返回值：

- 返回实际读写成功的元素个数

例子：

```c
fwrite(&stu1, sizeof(stu), 1, fp);
fread(&stu1, sizeof(stu), 1, fp);
```

注意：

- 二进制读写一般使用 `"rb"`、`"wb"`
- 返回值不是字节数，而是元素个数

**11. 文件位置操作**

函数：

```c
int fseek(FILE *stream, long offset, int whence);
long ftell(FILE *stream);
void rewind(FILE *stream);
```

作用：

- `fseek`：移动文件位置指针
- `ftell`：获取当前位置
- `rewind`：回到文件开头

`whence` 常见值：

| 常量       | 含义           |
| ---------- | -------------- |
| `SEEK_SET` | 从文件开头开始 |
| `SEEK_CUR` | 从当前位置开始 |
| `SEEK_END` | 从文件末尾开始 |

例子：

```c
fseek(fp, 0, SEEK_END);
long size = ftell(fp);
rewind(fp);
```

用途：

- 求文件大小
- 回到文件开头重新读取

**12. 刷新缓冲区**

函数：

```c
int fflush(FILE *stream);
```

作用：

- 强制把缓冲区内容写出去

例子：

```c
printf("请输入姓名: ");
fflush(stdout);
```

注意：

- 主要用于输出流
- 打印提示但不换行时很有用

**13. 文件结束和错误判断**

函数：

```c
int feof(FILE *stream);
int ferror(FILE *stream);
```

作用：

- `feof`：判断是否到达文件末尾
- `ferror`：判断是否发生读写错误

注意：

- 不要用 `while (!feof(fp))` 作为读文件循环条件
- 正确方法应该是先读，再判断返回值

正确写法：

```c
while (fgets(buf, sizeof(buf), fp) != NULL)
{
    fputs(buf, stdout);
}
```

或：

```c
while (fscanf(fp, " id:%d name:%53s score:%d", &s.id, s.name, &s.score) == 3)
{
    ...
}
```

**14. 常用错误处理**

函数：

```c
void perror(const char *s);
```

作用：

- 输出自定义提示和当前错误原因

例子：

```c
if (fp == NULL)
{
    perror("fopen");
}
```

## **3. 常用函数总结表**

| 函数      | 作用               |
| --------- | ------------------ |
| `fopen`   | 打开文件           |
| `fclose`  | 关闭文件           |
| `fgetc`   | 读一个字符         |
| `fputc`   | 写一个字符         |
| `fgets`   | 读一行             |
| `fputs`   | 写字符串           |
| `fscanf`  | 格式化读取         |
| `fprintf` | 格式化写入         |
| `fread`   | 按块读取二进制数据 |
| `fwrite`  | 按块写入二进制数据 |
| `fseek`   | 移动文件位置       |
| `ftell`   | 获取当前位置       |
| `rewind`  | 回到文件开头       |
| `fflush`  | 刷新缓冲区         |
| `feof`    | 判断文件结束       |
| `ferror`  | 判断读写错误       |
| `perror`  | 输出错误信息       |

## **4. 文件标准 IO 常见易错点**

- `fopen` 后忘记判空
- 用 `"w"` 打开文件导致原文件被清空
- `fclose` 后继续使用文件指针
- `fgetc` 返回值用 `char` 接，导致无法正确判断 `EOF`
- `fgets` 读入后忘记处理末尾 `\n`
- `fputs` 不会自动加换行
- `fscanf` 格式和文件内容不一致
- `%s` 不限制长度，容易越界
- 用 `while (!feof(fp))` 读文件
- 文本文件和二进制文件读写方式混用

## **5. 读文件的常见模板**

按行读文本：

```c
char buf[128];
while (fgets(buf, sizeof(buf), fp) != NULL)
{
    fputs(buf, stdout);
}
```

按格式读文本：

```c
while (fscanf(fp, " id:%d name:%53s score:%d", &s.id, s.name, &s.score) == 3)
{
    printf("id:%d name:%s score:%d\n", s.id, s.name, s.score);
}
```

按结构体读二进制：

```c
while (fread(&s, sizeof(s), 1, fp) == 1)
{
    printf("%d %d\n", s.id, s.score);
}
```



# 5.串口编程

## 1、串口通信

tx:发送数据线

rx：接收数据线

```
A(开发板串口)		B(模块)
TX<---->RX
RX<---->TX
VCC<---->VCC
GND<---->GND
```

linux中串口通信程序实现步骤：linux 一切皆文件

串口的操作

```
1.打开串口
    //6818串口所对应的文件名
    #define COM2_6818 "/dev/ttySAC1"
    #define COM3_6818 "/dev/ttySAC2"
    #define COM4_6818 "/dev/ttySAC3"

    //RK3568串口所对应的文件名
    #define COM0_3568 "/dev/ttyS0"
    #define COM1_3568 "/dev/ttyS1"
    #define COM3_3568 "/dev/ttyS3"
    #define COM4_3568 "/dev/ttyS4"
2.初始化串口
3.收发数据
	write
	read
4.关闭文件
```

## 2、GY39

有两种工作模式

```
1.只获取光照强度
2.只获取温湿度大气压强海拔
```

由外部控制器发送至 GY-39 模块（十六进制）

所有串口指令格式，帧头：0xa5 

指令格式：帧头+指令+校验和(8bit)  各占一个字节 共三个字节

指令：

10000001=>0x81 获取光照强度

10000010=>0x82 获取温湿度大气压强海拔

校验和(前面的数据累加的和，只会保留低8位)

0xa5+0x81 = 0x26

0xa5+0x82 = 0x27

char cmd[3] = {0xa5,0x81,0x26};

write(fd,cmd,3);

char cmd1[3] = {0xa5,0x82,0x27};



如果是第一种模式：

```c
接收数据，接收9个字节
char buf1[9] = {0};
read(fd,buf1,9);

buf[0] = 0x5a
buf[1] = 0x5a
buf[2] = 0x15

光照：
	Lux=(buf[4]<<24) | (buf[5]<<16) | (buf[6]<<8) | buf[7];
	Lux=Lux/100; (lux)
```

如果是第二种模式：

```
接收数据，接收15个字节
char buf2[15] = {0};
read(fd,buf2,15);
buf2[0] = 0x5a
buf2[1] = 0x5a
buf2[2] = 0x45

温度：
	T=(buf2[4]<<8)|buf2[5]; 
 	T=T/100 单位℃
气压：Byte6~Byte9 
     P=(buf2[6]<<24) | (buf2[7]<<16) | (buf2[8]<<8) | buf2[9] 
     P=P/100 单位 pa 
湿度：Byte10~Byte11 
     Hum=(buf2[10]<<8)|buf2[11]
     Hum=Hum/100 百分制
海拔：Byte12~Byte13
 	H=(buf2[12]<<8)|buf2[13] 单位 m
```

```c
void gy39()
{
	//打开串口
	
	//初始串口
	
	//获取光照强度
	int res = write(fd,cmd,3);
	if(res != 3)
	{
		return ;
	}
	
	char buf1[9] = {0};
	read(fd,buf1,9);
	if(buf1[0]==0x5a&&buf1[1]==0x5a&&buf1[2]==0x15)
	{
		Lux=(buf1[4]<<24) | (buf1[5]<<16) | (buf1[6]<<8) | buf1[7];
		Lux=Lux/100;
	}
	
	//切换模式
	int res = write(fd,cmd1,3);
	if(res != 3)
	{
		return ;
	}
    char buf2[15] = {0};
	read(fd,buf2,15);
	if(buf2[0]==0x5a&&buf2[1]==0x5a&&buf2[2]==0x45)
	{
		//温度
		T=(buf2[4]<<8)|buf2[5]; 
 		T=T/100;
 		//湿度 大气压强海拔 自己写代码
	}
}
```

也可以使用模式3，同时读取光照强度，温度，湿度....

```c
void GY39_2()
{
    //1.打开串口
    int ttyfd = open(COM2,O_RDWR);
    if(-1 == ttyfd)
    {
        perror("open ttyfd fail");
        return ;
    }
    //2.初始化串口
	init_tty(ttyfd);
	
    unsigned char rbuf1[15];
    int Lux,T,P,HUM,H;
    
    //获取
    //切换模式3 获取光照气压海拔等....
    ret = write(ttyfd,cmd2,3);
    if(ret!=3)
    {
        sleep(1);//等待完全写入 
    }

    read(ttyfd,rbuf2,3);//先读取3个字节
    if(rbuf1[0]==0x5A&&rbuf1[1]==0x5A&&rbuf1[2]==0x15)//光照
    {
    	read(ttyfd,rbuf2+3,9-3);//再读取6个字节
    	
    	Lux=(rbuf1[4]<<24)|(rbuf1[5]<<16)|(rbuf1[6]<<8)|rbuf1[7];
        data[0] = Lux=Lux/100;//最后一位不要 不显示小数
        printf("light = %d\n",Lux);
    	
    }
    else if(rbuf2[0]==0x5A&&rbuf2[1]==0x5A&&rbuf2[2]==0x45)//大气海拔温度湿度等...
    {
    	read(ttyfd,rbuf2+3,15-3);//再读取12个字节
		//温度
        T=(rbuf2[4]<<8)|rbuf2[5];
        data[1] = T=T/100;
        printf("T=%d\n",T);
        //气压湿度海拔自己完成 <---练习
		//气压
		P = (rbuf2[6]<<24)|(rbuf2[7]<<16)|(rbuf2[8]<<8)|rbuf2[9];
		data[2] = P = P/100;
		printf("P=%d\n",P);
		//湿度
		HUM=(rbuf2[10]<<8)|rbuf2[11];
        data[3] = HUM=HUM/100;
		printf("HUM=%d\n",HUM);
		//海拔
		H=(rbuf2[12]<<8)|rbuf2[13];
        data[4] = H=H;
		printf("H=%d\n",H);
    }
    close(ttyfd);
    return ;
}
```



## 3、MQ2

通信协议

![image-20241125124155886](C:\Users\Zelda\Desktop\Rk3568_Code\2_文件IO\03-串口编程\01-note\linux串口编程.assets\image-20241125124155886.png)

```c
unsigned char cmd[9] = {0xFF,0x01,0x86,0x00,0x00,0x00,0x00,0x00,0x79}; //传感器采集命令 用来发送数据
unsigned int concen = 0;//保存获取到的烟雾浓度的值
```


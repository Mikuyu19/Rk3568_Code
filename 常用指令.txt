开发板ip：192.168.31.13
虚拟机（Ubuntu）ip：192.168.67.138

写代码流程
1.写代码 xxx.c
2.编译(ubuntu)
	aarch64-linux-gnu-gcc xxx.c
	//a.out
3.传输可执行文件到开发板(看上面笔记)
	rz -y
	chmod +x 可执行文件
4.运行代码
	./可执行文件名


tftp指令
sudo service tftpd-hpa start	#启动tftp
sudo service tftpd-hpa stop		#停止tftp
sudo service tftpd-hpa restart	#重启tftp

注意：一般第一次传输文件 或 很久没有用tftp传输
	 需要重启tftp
	 
虚拟机网络图标没有解决方法
sudo service network-manager stop
sudo rm /var/lib/NetworkManager/NetworkManager.state
sudo service network-manager start
// 将文件里面唯一的false改成true
sudo gedit /etc/NetworkManager/NetworkManager.conf
sudo service network-manager restart

想要把a.out文件上传到开发板(网络通)
1.cp a.out /home/china/tftpboot
2.在secureCRT 
tftp -g -r main.c 172.50.1.92(ubuntu ip) 上传到开发板
tftp -p -l 1.c 	172.50.1.99 开发板把文件上传到ubuntu